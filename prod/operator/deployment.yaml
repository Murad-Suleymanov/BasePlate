apiVersion: apps/v1
kind: Deployment
metadata:
  name: easy-deploy-operator
  namespace: easy-deploy-system
  labels:
    app.kubernetes.io/name: easy-deploy-operator
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: easy-deploy-operator
  template:
    metadata:
      labels:
        app.kubernetes.io/name: easy-deploy-operator
    spec:
      serviceAccountName: easy-deploy-operator
      containers:
        - name: manager
          image: ghcr.io/murad-suleymanov/easy-deploy-operator:bc9c9b98a020ecb78dc3581f5186dc723a1c1643
          imagePullPolicy: Always
          args:
            - "--metrics-bind-address=:8080"
            - "--health-probe-bind-address=:8081"
          env:
            - name: ENVIRONMENT
              value: "prod"
            - name: BASE_DOMAIN
              value: "easysolution.work"
            - name: TARGET_IP
              value: "116.203.203.121"
            - name: REGISTRY_URL
              value: "registry.easysolution.work"
            - name: WEBHOOK_URL
              value: "https://webhook.easysolution.work"
          envFrom:
            - secretRef:
                name: github-pipeline-secret
                optional: true
          ports:
            - name: metrics
              containerPort: 8080
            - name: health
              containerPort: 8081
            - name: webhook
              containerPort: 9090
          livenessProbe:
            httpGet:
              path: /healthz
              port: health
          readinessProbe:
            httpGet:
              path: /readyz
              port: health
